# Book 设计风格与 UI 规范


---

## 设计气质

**墨水屏友好的 Material Lite / 阅读器工具页**

- 纯白画布，无装饰背景、无彩色主题
- 信息优先：封面 + 元数据 + KPI + 动作，一眼扫完
- 对比靠黑/灰阶，不用彩色强调
- 克制圆角与轻阴影，不堆边框
- 交互直给：点 hero 开读，底部等宽 chip，无花活

一句话：**白底、黑字、灰辅、圆角浅浮起、图标+文案并排。**

对齐口诀：

> **白底 + Material Outlined + 8px 圆角浅卡/胶囊 + 黑灰字阶 + 16 边距；块用卡、行用平铺；溢出分页不滚动。**

---

## 布局骨架

```
顶栏（返回 + 通栏底线）
  ↓
内容区（页边距 pad，顶对齐，禁止 ScrollableContainer）
  · Hero 书籍信息
  · KPI 三列 / 或书城完整简介
  · 「最近几天」平铺列表 + 必要时 Pager
  ↓
底栏动作行（等宽 chip / 书城单主按钮）
```

| 规则 | 值 / 说明 |
|------|-----------|
| 画布 | 全屏 `COLOR_WHITE` |
| 内容边距 | `UI.pagePad()` = 16（经 `ui_scale`） |
| 区块竖向间距 | 约 12 |
| 对齐 | 顶对齐；内容不满时**不**垂直居中 |
| 溢出 | 用 `Pager`，禁止页面内 `ScrollableContainer` |

---

## 色彩（灰度阶梯）

| 角色 | 来源 | 约值 |
|------|------|------|
| 页面底 | `COLOR_WHITE` | 纯白 |
| 主文案 | `COLOR_BLACK` | 纯黑 |
| 次文案（作者/简介/空态） | `UI.muted()` | ≈ `#33` |
| 更弱（分类·系列） | `UI.dim()` | ≈ `#44` |
| 分割线 | `UI.rule()` | ≈ `#55` |
| 卡片浅底 / 进度空轨 | `UI.surface()` / `UI.track()` | ≈ `#EE` |
| 轻阴影 | `COLOR_GRAY_D` | 偏移约 2px |
| 进度填充 / 角标底 | 纯黑；角标字白 | — |

无品牌色、无强调色、无夜间专用配色（跟墨水屏阅读习惯）。

---

## 字体与字号

逻辑字号，再经 `UI.face` / `UI.fontSize` 乘 `ui_scale`。

| 用途 | face / 字号 | 样式 |
|------|-------------|------|
| 书名 | `cfont` 16 | **加粗** |
| KPI 数值 | `cfont` 16 | 常规 |
| 节标题（简介） | `cfont` 15 | 常规黑 |
| 作者 / 进度% / 日时长 | `xx_smallinfofont` 12 | muted 或黑 |
| 副文案 / 简介摘要 | 11 | dim / muted |
| KPI 标签 | 11 | muted |
| 空态文案 | 13 | muted |
| 完整简介正文 | 14 | muted |
| 动作 chip 文案 | 14 | **不加粗** |

层级：**16 标题/KPI → 14–15 节/按钮 → 12 正文辅 → 11 最弱。**

---

## 表面组件（`Surface`）

| 形态 | 规则 |
|------|------|
| **Card** | 浅底 `#EE`、圆角 `UI.cardRadius()` = 8、默认轻阴影；封面 `clip=true` 圆角裁切 |
| **Pill** | 圆角 = 高度一半；详情动作 chip **无阴影** |
| Hero 外框 | `background=false`、`radius=0`、无阴影 — 信息条，不再套卡 |
| KPI | 有阴影小卡，上数值下标签，居中 |
| 「最近几天」 | **故意无卡片壳**，平铺行 |

原则：**卡片只包「可感知的数据块」；列表行与 hero 信息区不乱套卡。**

实现入口：`book.koplugin/ui/components/surface.lua`、尺寸/颜色积木在 `bookui.lua`。

---

## 图标

- 字体：**Material Symbols Outlined**（`ui/components/icon.lua`）
- 名字写 Material Icons 原名：`arrow_back` / `edit` / `search` / `play_arrow` …
- 顶栏返回：图标 24 +「返回」
- 动作 chip：图标 18 + 文案，横向，间距 6
- 图标与字同色阶（黑/灰），无彩色图标

---

## 组件形态（详情页）

| 组件 | 形态 |
|------|------|
| 顶栏 | 自绘：左对齐返回，高 48，底通栏细线；白底 |
| Hero | 左封面（约宽 22% 上限 80，**2:3**）+ 右栏等高；进度贴底「NN% + 圆角条」 |
| 封面 | 浅底圆角卡 + 轻阴影；letterbox；详情 hero **关**进度角标 |
| 进度条 | 高约 6–7，胶囊轨；空 `#EE`，满黑（`UI.progressBar`） |
| KPI | 三等分，间隙 10，内边距 10 |
| 日统计行 | 日期(muted) \| 条 \| 时长(黑)；行高 22，行距 8 |
| 底栏动作 | 等高 44 等宽 pill；间隙 10；图标+文案，整颗可点 |
| 书城主 CTA | `ButtonTable` 单钮「加入书库」（可有禁用态） |

---

## 交互与文案

- 主路径：点封面/信息区，或「开始/继续阅读」
- 次路径：编辑 / 刮削 / 优化排版 — 同级 chip，无主次色差（墨水屏靠位置与文案区分）
- 文案短、动词明确
- 空态一句 muted，不插图

---

## 刻意不做

- 彩色主题 / 渐变 / 大阴影 / 多层边框
- 页面内滚动
- 按钮加粗抢层级
- 列表行再套卡片
- Hero 再套有底有阴影的外框

---

## 锁屏账单柱图（阅读账单）

实现：`lockscreen/context.lua`（粒度）+ `styles/bill.lua` + `render.lua` 的 `vbar`。

对齐详情页「最近几天」与统计月柱的气质：**底对齐、黑柱、灰标签、不堆装饰。**

### 粒度（与其它账单统计同理）

| 周期 | `grain` | 柱轴 |
|------|---------|------|
| `today` | `hour` | 本地时区 0–23 时，补齐空档 |
| `7d` / `30d` / `month` | `day` | 周期内逐日，补齐空档 |

均分文案：按天 →「日均」（÷ 格数）；按小时 →「时均」（÷ 今日已过小时，含当前小时）。

### 绘制规则

| 规则 | 说明 |
|------|------|
| 满高浅轨 | **禁止**。旧实现每根柱背后一整条 `#EE`，周期一长就像栅栏 |
| 空档 | 不画柱，只留底沿线位置 |
| 有值柱 | 自底向上；相对周期峰值归一；最短 **3px**（墨水屏可见） |
| 柱宽 | 有上限（约内容宽 8%）；整组在内容区**水平居中** |
| 间隙 | ≥ 2px，约内容宽 1.2% |
| 圆角 | 半径 ≤ 3，**不是**整根半圆胶囊 |
| 底沿线 | 1px，色走 `rule`（≈ `#55`），贴柱脚 |
| 标签 | ≤7 格：每柱下标（日=`MM-DD`，时=`HH`，dim）；更多格：仅首尾 |
| 填充色 | 纯黑；标签 dim（≈ `#44`） |

`vbar` 契约：

- `track` 可选；锁屏柱图**不传**
- 调用方可直接给出实心柱的 `y/height` 且 `value = 1`（账单写法），也可在固定槽高里用 `value` 比例填充

---

## 关键文件

| 文件 | 职责 |
|------|------|
| `ui/desktop/detail.lua` | 详情页布局与行为（规范来源） |
| `ui/components/bookinfo.lua` | Hero / 封面 / 进度行 |
| `ui/components/surface.lua` | Card / Pill |
| `ui/components/bookui.lua` | 尺寸、颜色、进度条积木 |
| `ui/components/icon.lua` | Material 图标 |
| `ui/components/pager.lua` | 分页（替代滚动） |
| `lockscreen/styles/bill.lua` | 锁屏阅读账单（含日/时柱图） |
| `lockscreen/render.lua` | 锁屏离屏绘制（`panel` / `bar` / `vbar`） |
